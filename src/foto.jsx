import React from "react";
import { createRoot } from "react-dom/client";
import FotoApp from "./FotoApp.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FotoApp />
  </React.StrictMode>
);
