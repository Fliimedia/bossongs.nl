# Foto's worden altijd via api.php geserveerd, dus rechtstreekse toegang is dicht.
# Beide syntaxen, omdat niet elke server Apache 2.4 draait.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
